package com.example.notedesk.presentation.previewNote

import android.app.Application
import com.example.notedesk.presentation.util.NotesViewModel

class PerviewViewModel(application: Application) : NotesViewModel(application)